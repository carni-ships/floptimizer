# Optimization Cases

This directory is for longer case studies that connect a real optimization investigation to one or more trick cards.

Use it for:

- end-to-end before/after narratives
- explanations of surprising wins or failures
- writeups that justify promoting a trick from `candidate` to `curated`
